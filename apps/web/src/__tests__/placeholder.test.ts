import { describe, expect, it } from "bun:test";

describe("@return-school/web", () => {
  it("resolves as a workspace package", () => {
    // Placeholder — verifies the Vite + React + Tailwind app
    // is properly wired into the monorepo. Real component tests
    // will be added in S1–S5.
    expect(true).toBe(true);
  });
});
