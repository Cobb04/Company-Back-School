import { describe, expect, it } from "bun:test";

describe("@return-school/planning-core", () => {
  it("resolves as a workspace package", () => {
    // Placeholder — verifies the package is wired into the workspace.
    // Real tests for calculateSafeDepartureTime, scoreTrainCandidate,
    // and buildReturnPlans will be added in S1–S5.
    expect(true).toBe(true);
  });
});
